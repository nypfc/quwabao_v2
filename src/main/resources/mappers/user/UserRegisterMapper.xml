<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gedoumi.quwabao.user.mapper.UserRegisterMapper">

    <insert id="createUser" parameterType="User" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO user (error_count,
                          error_time,
                          last_login_ip,
                          last_login_time,
                          mobile_phone,
                          password,
                          real_name,
                          register_time,
                          update_time,
                          user_status,
                          username,
                          invite_code,
                          token,
                          deviceId,
                          reg_invite_code,
                          user_type,
                          sex,
                          eth_address,
                          validate_status,
                          id_card)
        VALUES (#{errorCount},
                #{errorTime},
                #{lastLoginIp},
                NOW(),
                #{mobilePhone},
                #{password},
                #{realName},
                NOW(),
                NOW(),
                #{userStatus},
                #{username},
                #{inviteCode},
                #{token},
                #{deviceId},
                #{regInviteCode},
                #{userType},
                #{sex},
                #{ethAddress},
                #{validateStatus},
                #{idCard})
    </insert>

    <select id="queryUserIdByInviteCode" resultType="Long">
        SELECT id
        FROM user
        WHERE invite_code = #{param1}
    </select>

</mapper>